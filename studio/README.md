# Estúdio OL Academy

Interface local para conduzir a produção de aulas da OL Academy. A aplicação visual roda em `localhost:3000` e conversa com o serviço local em `127.0.0.1:4318`.

Na raiz do projeto, use `npm run studio` para iniciar as duas partes juntas.

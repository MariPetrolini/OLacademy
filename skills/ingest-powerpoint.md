# Skill: ingest-powerpoint

Execute `npm run slides:ingest -- <lesson> --pptx <deck.pptx>`. Confirme `source/slides-manifest.json` e que todos os slides foram rasterizados. O PPT não é fonte técnica; é material visual obrigatório.
